import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "26 Free PDF Tools — Claira Slate",
  description: "Merge, compress, convert, sign and organize PDFs. 26 free tools, no account required, no watermarks.",
};

export default function ToolsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
